# Pathfinder v3 — Session Context for Claude

**Read this file at the start of every new session.**

## What Is Pathfinder

Pathfinder is a local-data-first job search copilot for senior product leaders. It helps them find relevant roles sooner, prepare faster, and make better career decisions. The architecture (v3) is standalone HTML modules served via HTTP on a combined Node.js server (port 3000) that serves both static files AND the data persistence bridge API with zero npm dependencies.

**Owner:** [Your Name] ([your-email])
**Local Path:** `~/Projects/job-search-agents-v3`
**PRD:** `docs/Pathfinder-PRD-v7.5.md` (canonical operating spec)
**Previous version:** `../` (v2 codebase, kept as reference but not active)

## v1 Scope (locked)

4 core modules + 1 cross-cutting capability + 1 orchestration surface:

| Surface | Path | Purpose | Key Data |
|---------|------|---------|----------|
| **Dashboard** | `modules/dashboard/` | Orchestration — routes user to core loop, surfaces next actions | Reads all `pf_*` keys |
| **Pipeline Tracker** | `modules/pipeline/` | Role CRUD, Kanban, stage management | `pf_roles`, `pf_companies` |
| **Job Feed** | `modules/job-feed/` | Discovery, scoring, intake approval | `pf_feed_queue`, `pf_preferences` |
| **Research Brief** | `modules/research-brief/` | 5 required + 9 expandable sections per role | Bridge: `/api/generate-section`, `/api/save-brief` |
| **Resume Tailor** | `modules/resume-tailor/` | Keyword match, bullet suggestions with provenance, export | `pf_bullet_bank`, `pf_resume_log` |
| **Artifacts Store** | MCP server (port 3847) | Storage + retrieval + citations, no standalone UI in v1 | MCP: `pf_save_artifact`, `pf_get_artifact`, etc. HTTP: `/api/artifacts/*`, `/api/citations/*` |

## Canonical Schemas (v7.5)

### Normalization Rules
- `compensation`: always `{ min, max, currency, type, raw }` — never use `salary`
- `level`: single field — never use `targetLevel`
- `stage` (Company): canonical taxonomy — never use `fundingStage`
- `scoring`: `{ titleFit, networkFit, domainFit, levelFit, companyFit, compensationFit, locationFit, jdFit }` — 8 dimensions, each 0-100. Weighted: domain 18%, title 15%, location 15%, network 12%, comp 12%, jdFit 10%, level 10%, company 8%. Generated by `score-engine.js` v3 using `processJD()` + `scoreJDFit()` + user prefs.
- `reasons`: `string[]` — one reason per scoring dimension, set alongside `scoring` by score-engine.js
- `companyStage` (FeedItem): transient intake field, mapped to `stage` on Company Record on approval
- `blockedCompanies`: array of `{ company: string, blockedAt: ISO8601 }` in `pf_preferences` / `pf_feed_preferences` — score engine returns 0 for blocked, feed filter hides them
- `lastActivity`: canonical timestamp for last update — never use `dateUpdated`

### Company Stage Taxonomy
`Public` | `Late-stage private` | `Growth-stage` | `Early-stage` | `Bootstrapped / Private` | `Unknown`

### Research Brief Sections
**Required (5):** snapshot, existence, plausible, screenOut, nextSteps
**Expandable (9):** pursuitEconomics, companyMarket, needs, fit, gaps, network, interview, proofPoints, dealBreaker

## Observability (Phoenix)

**Stack:** Arize Phoenix (Docker) + `@arizeai/phoenix-otel` (Node.js SDK)
**Start:** `docker compose up -d` → Phoenix UI at `http://localhost:6006`
**Design:** Graceful no-op — if Phoenix is down, OTEL exporter silently drops spans. Zero app impact.

**Instrumented modules:**
- `server.cjs` — root span per API request with method/path/status attributes; detailed attributes on `/api/generate-resume`, `/api/feed/process`, and `/api/fetch-jd` (URL, LinkedIn detection, char count, errors, fallback tracking)
- `scripts/feed-pipeline.js` — `runPipeline` parent span with all pipeline stats (including `enrichFallback` count); child spans for `stageEnrich` (JD fetching with per-item `enrichItem` spans), `stageExtract` (salary/location extraction), `stageScore` (batch scoring with avg score)
- `score-engine.js` — `scoreFeedItem` span with all 8 dimension scores + company/title/jdLength; `scoreAllFeedItems` batch span with size/avgScore
- `resume-generator.cjs` — `generateResume` and `generateGenericResume` spans with domain/keywords/bulletCount/outputFormat

**Config:**
- `PHOENIX_ENDPOINT` env var (default: `http://localhost:6006/v1/traces`)
- `PHOENIX_PROJECT` env var (default: `pathfinder`)
- `TRACING_DISABLED=1` to disable tracing entirely

## JD Fetch Cascade (v4.4.0)

**Endpoint:** `POST /api/fetch-jd` — accepts `{ url, company?, title? }`

**Flow:**
1. **Primary fetch:** LinkedIn guest API (`/jobs-guest/jobs/api/jobPosting/{jobId}`) for LinkedIn URLs, or direct HTTP GET for all others
2. **Search fallback (automatic):** If primary fails AND `company`+`title` provided, searches DuckDuckGo for `"{company}" "{title}" job`, ranks results by scrapable domain preference, tries top 5 until one yields a usable JD (200+ chars with job signal keywords)
3. **Response metadata:** `{ text, url, fetchedAt, charCount, fallback?, fallbackSourceUrl?, fallbackSourceDomain? }`

**Scrapable domain ranking:** Greenhouse > Lever > Ashby > Workday > SmartRecruiters > careers.* > jobs.* > Indeed > Glassdoor > BuiltIn > Wellfound > ZipRecruiter

**Pipeline integration:** `feed-pipeline.js` sends `{ url, company, title }` and tracks fallback provenance on items via `jdSource`, `jdSourceUrl`, `jdSourceDomain` fields.

## Mandatory Rules

1. **PRD sync rule:** Every code change must update the relevant component PRD, CHANGELOG, and this file.
2. **Schema discipline:** Use canonical field names only. No legacy aliases.
3. **Privacy visibility:** Every generation action must indicate whether data leaves the local system.
4. **Resume provenance:** Every AI suggestion must trace to bullet bank, base resume, or locally stored story-bank content.
5. **Graceful degradation:** Core non-AI workflows must work when bridge is down.
6. **No deferred modules in nav:** Only Dashboard + 4 core modules reachable from product navigation.

## File Structure

```
job-search-agents-v3/
├── CLAUDE_CONTEXT.md
├── CHANGELOG.md
├── start.sh
├── server.cjs                   (v2.0.0: static + data + artifacts + citations + briefs + backup + JD fetch + embeddings + vectors + tracing)
├── tracing.cjs                  (shared OTEL tracing module — Phoenix backend, graceful no-op)
├── docker-compose.yml           (Phoenix observability stack — port 6006)
├── package.json                 (npm deps: @xenova/transformers, onnxruntime-node, @arizeai/phoenix-otel, @opentelemetry/api)
├── docs/
│   └── Pathfinder-PRD-v7.5.md
├── bridge/
│   ├── src/
│   │   ├── bridge-standalone.ts (archived — failed due to sharp issue)
│   │   └── bridge-lite.cjs      (NEW: lightweight standalone fallback, unused)
│   └── package.json
├── mcp-server/                  (NEW: standalone MCP Artifacts server)
│   ├── src/
│   │   ├── index.ts             (dual transport: stdio + HTTP on port 3847)
│   │   ├── constants.ts         (shared config, artifact types, trust levels)
│   │   ├── types.ts             (TypeScript interfaces)
│   │   ├── services/storage.ts  (file-based storage engine)
│   │   ├── tools/artifacts.ts   (6 MCP artifact tools)
│   │   └── tools/citations.ts   (3 MCP citation tools)
│   ├── dist/                    (compiled JS output)
│   ├── package.json
│   └── tsconfig.json
├── .pathfinder-data/            (NEW: project-relative data dir instead of ~/.pathfinder)
└── modules/
    ├── shared/
    │   ├── pathfinder.css        (design system)
    │   ├── data-layer.js         (localStorage + bridge sync, now uses window.location.origin)
    │   ├── components.js         (nav, toast, modal, empty state, utils)
    │   ├── mcp-client.js         (NEW: PathfinderMCP class for MCP HTTP bridge on port 3847)
    │   ├── score-engine.js       (7-dimension feed scoring with JD enrichment, canonical SCORE_WEIGHTS)
    │   ├── text-utils.js         (processJD auto-extraction, HTML stripping, LinkedIn JD parsing)
    │   ├── email-parsers.js      (NEW v4.0: pluggable email parser registry — LinkedIn + Jobright parsers)
    │   ├── outreach-templates.js (NEW v4.0: 5 outreach message templates with formatOutreachDraft)
    │   ├── api-client.js         (bridge API client)
    │   ├── dedup-utils.js        (fuzzy dedup engine)
    │   ├── privacy-indicator.js  (privacy badges)
    │   ├── backup-utils.js       (export/import)
    │   └── command-palette.js    (Cmd+K global search, all modules)
    ├── dashboard/
    │   ├── index.html            (cache-busted data-layer.js?v=5.0.0)
    │   └── dashboard.js
    ├── pipeline/
    │   ├── index.html            (cache-busted data-layer.js?v=5.0.0)
    │   └── pipeline.js
    ├── job-feed/
    │   ├── index.html            (cache-busted data-layer.js?v=5.0.0)
    │   └── job-feed.js
    ├── research-brief/
    │   ├── index.html            (cache-busted data-layer.js?v=5.0.0)
    │   └── research-brief.js
    └── resume-tailor/
        ├── index.html            (cache-busted data-layer.js?v=5.0.0)
        └── resume-tailor.js
```

## Current State

**Version:** v4.2.0 — Blocked Companies + AdTech Premium Boost

### Agent Architecture (v4.0)

Five agents, file-based message passing (no direct agent-to-agent calls):

| Agent | Implementation | Trigger | Data |
|-------|---------------|---------|------|
| **Feed Scout** | Scheduled task `pathfinder-email-feed-scan` | Twice daily + manual | Writes `pf_feed_queue`, `pf_feed_runs` |
| **Resume Tailor** | Scheduled task `pathfinder-resume-generator` | Every 15 min (polls queue) | Reads `resume-requests/`, writes `generated-resumes/` |
| **Health Monitor** | Scheduled task `pathfinder-health-monitor` | Every 30 min | Writes `pf_health_report` |
| **Outreach Drafter** | Scheduled task `pathfinder-outreach-drafter` | Every 15 min (polls queue) | Reads `outreach-requests/`, updates in-place |
| **Pipeline Orchestrator** | `pipeline.js` (browser-side) | On page load + user actions | Reads/writes `pf_roles`, triggers other agents via request files |

Email sources: LinkedIn (`jobs-noreply`, `jobalerts-noreply`) + Jobright (`noreply@jobright.ai`)

Scoring weights (canonical, in score-engine.js v3): domainFit 18%, titleFit 15%, locationFit 15%, networkFit 12%, compensationFit 12%, jdFit 10%, levelFit 10%, companyFit 8%

Preferred industries: AdTech gets +18 boost (premium — core domain), all others (FinTech, AI/ML Platform, Data Platform, Internal/SalesOps) get +12. B2C penalty: -12.

Location tiers (from Walnut Creek): SF + East Bay + mid-peninsula = 100, Remote = 85, Hybrid near WC/SF = 80, Hybrid South Bay = 65, South Bay in-office = 55, Other CA = 35, Out-of-state = 20.

Noise floor caps: no JD + no salary → 35, no JD → 45, zero title match → 40.

See `docs/agent-instructions.md` for full agent specs.
**Last Updated:** 2026-03-31

### Implementation Status

| Module | Status | What Works | What's Missing |
|--------|--------|------------|----------------|
| Shared infrastructure | ✅ Complete | Geist Sans font, CSS design tokens, data-layer, components, skeleton loading, nav with a11y, dedup, privacy, backup, API client | — |
| Server (combined) | ✅ Complete | Static file serving (port 3000) + bridge API on same port, local embeddings (all-MiniLM-L6-v2), in-memory vector store, data sync, **binary file upload (multipart)**, artifact download with legacy path resolution. npm deps: @xenova/transformers, onnxruntime-node | — |
| Bridge | ✅ Consolidated | All endpoints on port 3000. MCP server (3847) kept for Claude Code stdio only | — |
| Dashboard | ✅ Complete | Pipeline summary, nudges, streak counter, **smart quick actions** (context-driven: interviews, offers, stale, outreach, feed matches), time-in-stage, conversion funnel, activity trend, **network advantage** (top 5 companies by connections), empty state, loading skeletons, a11y, **local backup/restore** (export/import JSON), **command palette** (Cmd+K) | — |
| Pipeline Tracker | ✅ Complete | Kanban board, drag-drop, add/edit/delete roles, search, detail panel, favicons, -2px hover lift, connections/location/source/URL on cards, **next-action on cards**, stage history timeline, comms log (contact + outcome), comp estimation, **stage-specific close reasons**, **closed column toggle**, stageHistory, **artifact file manager**, editable source field, scoring breakdown, **semantic search toggle** (🧠), **company enrichment** (auto + manual), **company news cache** (24h TTL), **LinkedIn URLs on connections**, **substage selector**, **cross-tab sync**, **command palette** (Cmd+K) | Keyboard nav (J/K) |
| Job Feed | ✅ Complete | Feed cards with side panel, JD-powered 8-dim scoring, auto-score, approve with tier suggestion, dismiss/snooze, sort (6 modes incl. **network sort**), search, filters, preferences, comp estimation, 3-column layout, skeletons, a11y, **fuzzy dedup** (Levenshtein via dedup-utils.js), **dismissal pattern tracking**, **network count badges**, **feed run history** (collapsible), **scoring recalibration** (learns from conversions + dismissals), **blocked companies** (chip UI + one-click block from cards, fuzzy match, auto-dismiss), **command palette** (Cmd+K) | — |
| Research Brief | ✅ Complete (no AI) | Role selector, JD paste, 14 sections, brief save/load/cache, section defs, Cowork prompt templates | AI generation (use Cowork) |
| Resume Tailor | ⚠️ Degraded | 3-phase workflow, keyword grouping, bullet locks, version history, bullet bank | AI generation (use Cowork), DOCX/PDF export |

### PRD Reconciliation (2026-03-29)

Full reconciliation completed against PRD v4.4.2. See `tasks/prd-reconciliation.md` for the 33-gap analysis.

**Fixed in this pass (Batch A+B, 13 items):**
- Font: Inter → Geist Sans (§6.1)
- Favicons on all 5 modules (§6.8)
- Dashboard: 4 quick actions, streak counter, time-in-stage, dismissed nudge filtering (§7.6)
- Job Feed: Snooze action with 3-day expiry (§7.5)
- Pipeline: Card hover -2px lift + shadow-md (§6.3.2)
- Page load fade-in on all modules (§6.8)
- Loading skeleton screens on all modules (§6.8)
- Accessibility pass: aria-live, role=main, aria-labels, nav landmarks (§6.4)

**Deferred (future batches):**
- Keyboard shortcuts J/K nav in Pipeline (§6.6)
- Research Brief expand to 13 sections (§7.2)
- Resume Tailor DOCX/PDF export (§7.3)
- 6 unported modules: Calendar, Outreach, Debrief, Comp, Mock Interview, Metrics
- Server endpoints: `/api/company-news`, `/api/company-stage-infer`, `/api/enrich-company` (client code ready)

### Architecture Changes (v3)

**Port Setup:**
- **Port 3000:** All services (static files, artifacts, citations, briefs, backup, JD fetch, embeddings, vectors)
- **Port 3847:** Optional, Claude Code stdio only
- **Port 8080:** V2 server (legacy, for reference)

**Server Consolidation (v3.7.0):** All MCP endpoints consolidated into server.cjs on port 3000. Artifacts, citations, briefs, backup/restore, JD fetch all served from one process.

**Embeddings (v3.8.0):** Local transformer embeddings via @xenova/transformers with sharp stubbed out. In-memory vector store for semantic search. Model: all-MiniLM-L6-v2 (384 dims). Lazy-loaded on first API call.

**Bridge Evolution:**
- `bridge/src/bridge-standalone.ts` — Failed (sharp native binary issues with @xenova/transformers)
- `bridge/src/bridge-lite.cjs` — Created as fallback but unused (zero-dep approach favored)
- `server.cjs` — NOW: full MCP infrastructure server (v2.0.0) replaces standalone bridge entirely

**Data Layer:**
- Changed bridge URL from hardcoded `localhost:3458` to `window.location.origin` for dynamic routing
- Added `pushToBridgeIfEmpty()` function: syncs localStorage → bridge on first load when bridge has no data
- Data directory moved from `~/.pathfinder/data/` to `.pathfinder-data/` (project-relative) for VM/Mac sharing

**Cache Busting:**
- All module HTML files now reference `data-layer.js?v=5.0.0` to prevent stale JS from being served

### Known Issues
- VM ports are NOT forwarded to Mac — only ports with Mac-native servers work (e.g., port 8080 with Python server in v2)
- Research Brief and Resume Tailor generation via Cowork sessions (user copies Cowork-ready prompt templates from app)
- LinkedIn connections recovered from bridge on pipeline init; if bridge is down, connections won't load until next bridge-available session
- Vector store is in-memory — resets on server restart. Roles must be re-indexed via POST /api/vectors/index-roles after restart.

## How to Start the Application

**Option 1: With Node.js available (recommended)**
```bash
cd ~/Projects/job-search-agents-v3
./start.sh
```
This starts the combined server on port 3000 (static + bridge API integrated) and the MCP Artifacts server on port 3847.

**Option 1b: MCP server only (for Claude Code stdio mode)**
```bash
node mcp-server/dist/index.js
```
Runs MCP server in stdio mode for Claude Code / Cowork integration (no HTTP).

**Option 2: Python fallback (legacy)**
```bash
python3 -m http.server 8080 --directory modules
```
This serves static files on port 8080 but has no bridge API (v2 approach).

**Access:**
- http://localhost:3000 (v3 — combined server)
- http://localhost:8080 (v2 — legacy, for reference)

## How to Start a New Session
1. Read this file (CLAUDE_CONTEXT.md)
2. Read CHANGELOG.md for recent changes
3. Read `docs/Pathfinder-PRD-v7.5.md` for requirements
4. Check port 3000 is running (combined server) or fall back to port 8080 (v2)
5. Ask what the user wants to do
